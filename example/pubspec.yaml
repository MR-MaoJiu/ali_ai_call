name: ali_ai_call_example
description: "Demonstrates how to use the ali_ai_call plugin."
publish_to: 'none'
version: 1.0.0
homepage: "https://github.com/MR-MaoJiu/ali_ai_call"
environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ali_ai_call:
    path: ../
  dio: ^5.4.0
  # 升级以适配 Flutter 移除 v1 Android embedding（避免使用 PluginRegistry.Registrar）
  shared_preferences: ^2.3.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
